{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permissions for the main Visuales desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:app:allow-set-app-theme", "dialog:default"]
}
