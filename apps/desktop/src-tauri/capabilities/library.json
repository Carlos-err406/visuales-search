{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "library-windows",
  "description": "Local library browser and preview windows sharing the native host.",
  "windows": ["library-*"],
  "permissions": ["core:default", "core:app:allow-set-app-theme", "dialog:default"]
}
